# (really just for install target)

include ../../makefile.inc

all:

.PHONY: install
install:
	cp upgrade_firmware $(MPBOX_INSTALL_DIR)/mpservice/bin
	chmod 755 $(MPBOX_INSTALL_DIR)/mpservice/bin/upgrade_firmware

clean_all:

clean:
