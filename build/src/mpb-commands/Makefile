# (really just for install target)

include ../../makefile.inc

all:

.PHONY: install
install:
	cp mpb-system $(MPBOX_INSTALL_DIR)/mpservice/cgi-bin
	chmod 755 $(MPBOX_INSTALL_DIR)/mpservice/cgi-bin/mpb-system

clean_all:

clean:
